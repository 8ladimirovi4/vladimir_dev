/** @layer widgets / slice philosophy / segment ui — server shell */

import { PrinciplesList } from './principles-list';

export function Philosophy() {
  return (
    <section data-widget="philosophy" id="philosophy">
      <PrinciplesList />
    </section>
  );
}
