'use client';

/** @layer features / slice project-filter / segment ui — client (read ?stack=) */

export function ProjectFilter({ children }: { children?: React.ReactNode }) {
  return <>{children}</>;
}
